class Solution {
    public List<Boolean> prefixesDivBy5(int[] nums) {
        ArrayList<Boolean> list = new ArrayList<>();
        
        int value = 0;

        for (int bit : nums) {
            value = (value * 2 + bit) % 5;   
            list.add(value == 0);
        }

        return list;
    }
}
